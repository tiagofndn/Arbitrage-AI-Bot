# AI Arb Lab - Environment Configuration
# Copy to .env and adjust as needed. Never commit .env with secrets.

# =============================================================================
# APPLICATION
# =============================================================================
APP_ENV=development
LOG_LEVEL=INFO
LOG_FORMAT=json

# Correlation ID prefix for request tracing (e.g., "arb-")
CORRELATION_ID_PREFIX=arb

# =============================================================================
# DATA
# =============================================================================
# Path to sample/synthetic data directory
DATA_DIR=./data/sample

# Synthetic generator defaults
SYNTHETIC_SEED=42
SYNTHETIC_VENUES=2
SYNTHETIC_DAYS=1

# =============================================================================
# BACKTESTING
# =============================================================================
BACKTEST_INITIAL_CAPITAL=100000.0
BACKTEST_COMMISSION_RATE=0.001

# =============================================================================
# PAPER TRADING (simulation only - no real execution)
# =============================================================================
PAPER_INITIAL_CAPITAL=100000.0
PAPER_MODE=simulated

# =============================================================================
# OPTIONAL SERVICES (Docker Compose)
# =============================================================================
# PostgreSQL - for persistent storage (optional)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=ai_arb_lab
# POSTGRES_USER=arb
# POSTGRES_PASSWORD=

# Redis - for caching / message queue (optional)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# =============================================================================
# OBSERVABILITY (stubs - OpenTelemetry)
# =============================================================================
# OTEL_EXPORTER_OTLP_ENDPOINT=
# OTEL_SERVICE_NAME=ai-arb-lab

# =============================================================================
# EXCHANGE CONNECTORS (STUBBED - DO NOT USE FOR LIVE TRADING)
# =============================================================================
# These are interface placeholders. Real implementations require:
# - Exchange API keys (never commit)
# - Compliance with exchange ToS and rate limits
# - Explicit opt-in and understanding of risks
# EXCHANGE_API_KEY=
# EXCHANGE_API_SECRET=
# EXCHANGE_SANDBOX=true

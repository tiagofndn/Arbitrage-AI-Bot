# Prometheus config for AI Arb Lab (optional observability)
scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  # Add scrape targets when metrics endpoint is enabled
  # - job_name: 'ai-arb-lab'
  #   static_configs:
  #     - targets: ['host.docker.internal:9091']
